<!DOCTYPE html>
<html>
<head>
  <title>Blig</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header>
<div class="user_stat_bar">
<% if signed_in? %>
<%= current_user.email %>
<%= link_to '<div class="signin_out_btn">Sign out</div>'.html_safe, sign_out_path, method: :delete %>
<% else %>
<%= link_to '<div class="user_name_btn">Welcome to BligBlog</div>'.html_safe, './'  %>
<%= link_to '<div class="signin_out_btn">Sign in</div>'.html_safe, sign_in_path %>
<% end %>
</div>
<div id="flash">
<% flash.each do |key, value| %>
  <div class="flash <%= key %>"><%= value %></div>
<% end %>
</div>
</header>

<%= yield %>

</body>
</html>
